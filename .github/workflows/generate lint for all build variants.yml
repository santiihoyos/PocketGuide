name: lint-generation

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

	  - name: GitHub Action for Android Lint
  		uses: yutailang0119/action-android-lint@v1.1.0

